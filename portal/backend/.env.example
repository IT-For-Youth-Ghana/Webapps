# ======================
# ITFY Portal API Configuration
# ======================

# Server Configuration
NODE_ENV=development
PORT=5000
HOST=0.0.0.0
API_PREFIX=/api

# Database Configuration (PostgreSQL recommended for production)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=itfy_portal
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_SSL=false
DB_POOL_MAX=20
DB_POOL_MIN=5
DB_POOL_ACQUIRE=30000
DB_POOL_IDLE=10000

# Database Sync (Development Only - NEVER ENABLE IN PRODUCTION)
# DB_SYNC=false                   # Keep false in production
# DB_SYNC_ALTER=false             # Keep false in production
# DB_SYNC_FORCE=false             # Keep false in production

# JWT Authentication (Use strong secrets in production)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-replace-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# SSO Configuration
SSO_SECRET=shared-secret-with-incubator-and-moodle-replace-in-production
SSO_EXPIRES_IN=5m

# Redis Caching (Enable in production for performance)
REDIS_ENABLED=true
REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Rate Limiting Configuration
RATE_LIMIT_ENABLED=true
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Moodle Integration
MOODLE_URL=https://lms.yourdomain.com
MOODLE_TOKEN=your_moodle_web_service_token
MOODLE_SYNC_INTERVAL=60000
MOODLE_BATCH_SIZE=50

# Paystack Payment Gateway
PAYSTACK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
PAYSTACK_PUBLIC_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
PAYSTACK_CALLBACK_URL=https://yourdomain.com/dashboard/payments/verify

# Incubator/Jobs Platform Integration
INCUBATOR_URL=https://incubator.yourdomain.com
INCUBATOR_SECRET=shared_secret_key_with_incubator

# Email Configuration
EMAIL_PROVIDER=smtp
EMAIL_FROM_NAME=IT For Youth Ghana
EMAIL_FROM_ADDRESS=noreply@itforyouthghana.org

# SMTP Settings (Production email service)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# Security Configuration
BCRYPT_ROUNDS=12

# Logging Configuration
LOG_LEVEL=info
LOG_FORMAT=combined

# Test Email (for development)
TEST_EMAIL=admin@yourdomain.com

# ======================
# PRODUCTION CHECKLIST
# ======================
# Before deploying to production, ensure:
# 1. NODE_ENV=production
# 2. Strong JWT_SECRET (min 32 chars, randomly generated)
# 3. Secure DB_PASSWORD
# 4. REDIS_ENABLED=true for caching
# 5. HTTPS URLs for FRONTEND_URL and PAYSTACK_CALLBACK_URL
# 6. Valid SMTP credentials for email delivery
# 7. DB_SSL=true if using cloud database
# 8. Proper CORS_ORIGINS for your domain
# 9. LOG_LEVEL=warn or error for production
# 10. BCRYPT_ROUNDS=12 or higher
